bmi = 84 / 1.65 ** 2
print(bmi)

print(int(bmi)) # Rounds down to the closest whole number

print(round(bmi)) # Rounds to the nearest whole number

print(round(bmi, 2)) # Rounds to the nearest number with however many decimal places are specified, i.e "2"

score = 0

score += 1 # Can be done with all operands
print(score)